[profile.default]
src = "src"
out = "out"
test = "test"
libs = ["lib"]
# fork tests read MAINNET_RPC_URL from the environment (never committed)
ffi = false

[rpc_endpoints]
mainnet = "${MAINNET_RPC_URL}"
